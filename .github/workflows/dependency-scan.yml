# packages - issues ya vulnerabilities (scan)
# pip-audit

name: Dependecy Scan

on:
    workflow_call:

jobs:

    dependency-check:

        runs-on: ubuntu-latest
        steps: 
            - name: Code Checkout
              uses: actions/checkout@v4

            - name: setup python 3.12
              uses: actions/setup-python@v6